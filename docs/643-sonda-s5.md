# sonda #643 (s5)

Documento novo que existe só para a medição do cenário s5 do holdout da issue #643: o diff desta
branch contra `main` é exatamente a remoção de um arquivo sob `src/**` mais este arquivo sob
`docs/`. A branch é apagada ao fim da medição.
